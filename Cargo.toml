[package]
name = "amd64"
description = "Firework AMD64 implementation crate"
repository = "https://bitbucket.org/firework-os/amd64"
keywords = ["amd64"]
categories = ["no-std"]
version = "0.1.0"
authors = ["VisualDevelopment"]
edition = "2021"
license-file = "LICENSE"

[profile.dev]
lto = "thin"

[profile.release]
lto = "thin"

[dependencies]
modular-bitfield = "0.11.2"
bit = "0.1.1"
amd64_macros = { path = "macros" }
